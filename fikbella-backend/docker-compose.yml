services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fikbella
      POSTGRES_PASSWORD: fikbella123
      POSTGRES_DB: fikbella_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: